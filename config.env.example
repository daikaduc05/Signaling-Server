# Database Configuration
DATABASE_URL=postgresql://user:password@localhost/signaling_db

# JWT Secret Key (change this in production)
SECRET_KEY=your-secret-key-here-change-in-production

# Optional: Logging Level
LOG_LEVEL=INFO

# Gmail SMTP Configuration for OTP Emails
# Để sử dụng Gmail SMTP, bạn cần tạo "App Password" từ Google Account:
# 1. Vào Google Account > Security > 2-Step Verification > App passwords
# 2. Tạo App Password mới cho "Mail"
# 3. Sử dụng App Password này cho SMTP_PASSWORD (không phải mật khẩu Gmail thường)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password-from-google
FROM_EMAIL=your-email@gmail.com